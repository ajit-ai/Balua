[package]
name = "balua-test"
version.workspace = true
edition.workspace = true
description = "Balua test runner — compile-and-run harness asserting exit codes"

[dependencies]
baluac = { path = "../../crates/baluac" }
anyhow.workspace = true

