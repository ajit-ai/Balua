[package]
name = "balua-fmt"
version.workspace = true
edition.workspace = true
description = "Balua code formatter — AST-adjacent token formatter preserving @hw:: alignment"

[dependencies]
baluac = { path = "../../crates/baluac" }

