# balua-prof

Profiler — perf/VTune/Tracy + Nsight + Vivado timing ? unified flame graph (Section 8.1 / 9.1).

Stub: implementation lives in Rust under this folder. Run with `cargo run -p balua-prof`.

